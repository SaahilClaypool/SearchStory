@page "/"

<div>
    <button @onclick="Clear">
        Clear
    </button>
</div>
<Add />
<All />
<Search />

@code {
    [Inject] public ClearAll ClearAll { get; set; }
    [Inject] public ILogger<Index> Logger { get; set; }
    public void Clear()
    {
        Logger.LogInformation("Clearing");
        ClearAll.Exectute(new());
    }
}